/*
Reescreva o exercício 50 utilizando a estrutura REPITA e um CONTADOR. 
*/

// Declaração do contador
var contador = 1;

// Estrutura do...while para repetir até que o contador seja maior que 10
do {
    console.log(contador);
    contador++;
} while (contador <= 10);
