/*
Reescreva o exercício 51 utilizando a estrutura ENQUANTO e um CONTADOR
*/

// Declaração do contador
var contador = 10;

// Estrutura while para repetir até que o contador seja menor que 1
while (contador >= 1) {
    console.log(contador);
    contador--;
}
