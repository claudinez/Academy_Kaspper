// Definindo as variáveis
const salarioBase = 3000; // Salário base em reais
const bonificacao = 500;  // Bonificação em reais

// Calculando o salário total
const salarioTotal = salarioBase + bonificacao;

// Exibindo o resultado no console
console.log("Salário Base: R$" + salarioBase);
console.log("Bonificação: R$" + bonificacao);
console.log("Salário Total: R$" + salarioTotal);