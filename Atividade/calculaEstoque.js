// Definindo as variáveis
const precoUnitario = 50; // Preço unitário em reais
const quantidade = 10;    // Quantidade de itens

// Calculando o custo total
const custoTotal = precoUnitario * quantidade;

// Exibindo o resultado no console
console.log("Preço Unitário: R$" + precoUnitario);
console.log("Quantidade: " + quantidade);
console.log("Custo Total: R$" + custoTotal);